name: experiments_riverpod
description: Experimenting with Riverpod

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:

  flutter:
    sdk: flutter

  flutter_riverpod:
    path: ../river_pod/packages/flutter_riverpod/
  freezed_annotation:

  uuid:

dev_dependencies:

  flutter_test:
    sdk: flutter

  # Generators
  build_runner:
  freezed:

flutter:

  uses-material-design: true
